# ============================================
# STAGING/DEV ENVIRONMENT - TEMPLATE
# ============================================
# Copy this file to your server at ~/ai_models_tgbot/.env.staging
# and fill in the values (copy most from .env.production)
#
# Used for: dev.webapp.vseonix.com
# Uses: separate test bot + shared production database

NODE_ENV=staging
PORT=3000

# Database (COPY FROM .env.production - same database)
DATABASE_URL=postgresql://aibot:YOUR_DB_PASSWORD@postgres:5432/aibot_production

# Redis (shared with production)
REDIS_HOST=redis
REDIS_PORT=6379

# Telegram TEST Bot Token (from BotFather - your test bot)
BOT_TOKEN=YOUR_TEST_BOT_TOKEN_HERE

# API Keys (COPY FROM .env.production)
OPENAI_KEY_1=
REPLICATE_KEY_1=
RUNWARE_KEY=
AIMLAPI_KEY=
KIEAI_KEY=
PIAPI_KEY=
OPENROUTER_KEY=
ELEVENLABS_KEY_1=
KLING_KEY_1=

# Logging
LOG_LEVEL=debug

# Queue Workers — DISABLED for dev-bot (dedicated worker handles all jobs)
# Set in docker-compose.prod.yml: DISABLE_WORKERS=true
# The dedicated worker (aibot_worker) processes jobs for ALL bot instances,
# using botToken from each job to reply via the correct bot.
DISABLE_WORKERS=true
